import { {{ pascalCase name }} } from './{{ pascalCase name }}.tsx'
import type {Meta, StoryObj} from '@storybook/react'

type T = typeof {{ pascalCase name }}
type Story = StoryObj<T>

const meta: Meta<T> = {
  component: {{ pascalCase name }},
}
export default meta

export const Default: Story = {
  args: {}
}
