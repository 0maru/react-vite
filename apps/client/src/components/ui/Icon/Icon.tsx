import type { FC, ReactNode } from 'react';

type IconProps = {
  children: ReactNode;
};

export const Icon: FC<IconProps> = ({}) => {
  return <div></div>;
};

Icon.displayName = 'Icon';
